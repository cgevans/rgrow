fn main() {
    eprintln!("WARNING: rgrow-gui is deprecated and has been merged into rgrow.");
    eprintln!("Install rgrow instead: cargo install rgrow");
    eprintln!("Or for Python: pip install rgrow");
    std::process::exit(1);
}
