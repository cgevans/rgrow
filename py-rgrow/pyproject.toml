[project]
name = "rgrow"
dependencies = ["numpy", "attrs"]
requires-python = ">=3.9"

[build-system]
requires = ["maturin~=1.4"]
build-backend = "maturin"

[tool.pytest.ini_options]
norecursedirs = [
    "dist",
    "build",
    ".tox"]
testpaths = ["tests"]