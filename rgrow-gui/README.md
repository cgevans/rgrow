# rgrow-gui (DEPRECATED)

**This package has been deprecated.** The functionality of `rgrow-gui` has been merged into the main [`rgrow`](https://crates.io/crates/rgrow) package.

Please use `rgrow` instead:

- **Cargo:** `cargo install rgrow` (the GUI is included by default)
- **pip:** `pip install rgrow`
